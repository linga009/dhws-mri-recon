# DHWS-MRI-Recon — Python dependencies
# Python 3.10+ recommended.   Install:  pip install -r requirements.txt
#
# For GPU PyTorch, install the CUDA build first from https://pytorch.org
# (e.g. pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121),
# then run this file for the rest.

# ── Core deep learning ───────────────────────────────────────────
torch>=2.0
torchvision>=0.15
numpy>=1.24

# ── fastMRI data: HuggingFace download + HDF5 reading ────────────
huggingface_hub>=0.20
datasets>=2.14
h5py>=3.8

# ── Image I/O ────────────────────────────────────────────────────
Pillow>=9.0
